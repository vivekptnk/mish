#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>

#define DELIM "\t\v\f\n\r"

int readline();
int remove_whitespace();
int stringify();

int cd();
int pwd();
int log_file();
int fork_exec();
int exit_cmd();

int readline(){
	char *line;
	size_t bufsize = 1024;
	t commands;
	
	line  = (char *)malloc(bufsize * sizeof(char));
	if (line == NULL){
		perror("Unable to allocate buffer");
		exit(1);
	}

	commands = getline(&line, &bufsize, stdin);
	printf("%zu characters were read. \n", commands);
	printf("You Typed: '%s'\n", line);

	return(0);
}


int remove_whitespace(size_t commands){
	size_t *bufcopy = commands;

	char tokens[1024];
	int i = 0;

	while(1){
		tokens[i] = strsep(&bufcopy, DELIM);
		if (tokens[i] == NULL) break; i++;
	}

	return bufcopy;
	//char command_str[1024]; 
	//char *ptr = strtok(commands, DELIM)

	//while (ptr != NULL){
	//	printf()
//	}



}
int main(){
	readline();
}
